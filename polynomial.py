import numpy as np


def f(x, T, name="_"):
  """ f(x) = x """
  return x, False, f"{name}"


def f2(x, T, name="_"):
  """ f(x) = x^2 """
  return x**2, False, f"{name}**2"


def f3(x, T, name="_"):
  """ f(x) = x^3 """
  return x**3, False, f"{name}**3"


def f4(x, T, name="_"):
  """ f(x) = x^4 """
  return x**4, False, f"{name}**4"


def f5(x, T, name="_"):
  """ f(x) = x^5 """
  return x**5, False, f"{name}**5"


def f6(x, T, name="_"):
  """ f(x) = x^6 """
  return x**6, False, f"{name}**6"


def f7(x, T, name="_"):
  """ f(x) = x^7 """
  return x**7, False, f"{name}**7"


def f8(x, T, name="_"):
  """ f(x) = x^8 """
  return x**8, False, f"{name}**8"


def f9(x, T, name="_"):
  """ f(x) = x^9 """
  return x**9, False, f"{name}**9"


def f10(x, T, name="_"):
  """ f(x) = x^10 """
  return x**10, False, f"{name}**10"


def f11(x, T, name="_"):
  """ f(x) = x^11 """
  return x**11, False, f"{name}**11"


def f12(x, T, name="_"):
  """ f(x) = x^12 """
  return x**12, False, f"{name}**12"


def f13(x, T, name="_"):
  """ f(x) = x^13 """
  return x**13, False, f"{name}**13"


def f14(x, T, name="_"):
  """ f(x) = x^14 """
  return x**14, False, f"{name}**14"


def f15(x, T, name="_"):
  """ f(x) = x^15 """
  return x**15, False, f"{name}**15"


def f16(x, T, name="_"):
  """ f(x) = x^16 """
  return x**16, False, f"{name}**16"


def f17(x, T, name="_"):
  """ f(x) = x^17 """
  return x**17, False, f"{name}**17"


def f18(x, T, name="_"):
  """ f(x) = x^18 """
  return x**18, False, f"{name}**18"


def f19(x, T, name="_"):
  """ f(x) = x^19 """
  return x**19, False, f"{name}**19"


def f20(x, T, name="_"):
  """ f(x) = x^20 """
  return x**20, False, f"{name}**20"