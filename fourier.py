import numpy as np
import torch

def sin(x, T, name="_"):
  """ f(x) = sin(x) """
  return torch.sin((1/T)*x), True, f"sin({name})"


def cos(x, T, name="_"):
  """ f(x) = cos(x) """
  return torch.cos((1/T)*x), True, f"cos({name})"


def sin2x(x, T, name="_"):
  """ f(x) = sin(2x) """
  return torch.sin(2*(1/T)*x), True, f"sin(2*{name})"


def cos2x(x, T, name="_"):
  """ f(x) = cos(2x) """
  return torch.cos(2*(1/T)*x), True, f"cos(2*{name})"


def sin3x(x, T, name="_"):
  """ f(x) = sin(3x) """
  return torch.sin(3*(1/T)*x), True, f"sin(3*{name})"


def cos3x(x, T, name="_"):
  """ f(x) = cos(3x) """
  return torch.cos(3*(1/T)*x), True, f"cos(3*{name})"


def sin4x(x, T, name="_"):
  """ f(x) = sin(4x) """
  return torch.sin(4*(1/T)*x), True, f"sin(4*{name})"


def cos4x(x, T, name="_"):
  """ f(x) = cos(4x) """
  return torch.cos(4*(1/T)*x), True, f"cos(4*{name})"


def sin5x(x, T, name="_"):
  """ f(x) = sin(5x) """
  return torch.sin(5*(1/T)*x), True, f"sin(5*{name})"


def cos5x(x, T, name="_"):
  """ f(x) = cos(5x) """
  return torch.cos(5*(1/T)*x), True, f"cos(5*{name})"


def sin6x(x, T, name="_"):
  """ f(x) = sin(6x) """
  return torch.sin(6*(1/T)*x), True, f"sin(6*{name})"


def cos6x(x, T, name="_"):
  """ f(x) = cos(6x) """
  return torch.cos(6*(1/T)*x), True, f"cos(6*{name})"


def sin7x(x, T, name="_"):
  """ f(x) = sin(7x) """
  return torch.sin(7*(1/T)*x), True, f"sin(7*{name})"


def cos7x(x, T, name="_"):
  """ f(x) = cos(7x) """
  return torch.cos(7*(1/T)*x), True, f"cos(7*{name})"


def sin8x(x, T, name="_"):
  """ f(x) = sin(8x) """
  return torch.sin(8*(1/T)*x), True, f"sin(8*{name})"


def cos8x(x, T, name="_"):
  """ f(x) = cos(8x) """
  return torch.cos(8*(1/T)*x), True, f"cos(8*{name})"


def sin9x(x, T, name="_"):
  """ f(x) = sin(9x) """
  return torch.sin(9*(1/T)*x), True, f"sin(9*{name})"


def cos9x(x, T, name="_"):
  """ f(x) = cos(9x) """
  return torch.cos(9*(1/T)*x), True, f"cos(9*{name})"


def sin10x(x, T, name="_"):
  """ f(x) = sin(10x) """
  return torch.sin(10*(1/T)*x), True, f"sin(10*{name})"


def cos10x(x, T, name="_"):
  """ f(x) = cos(10x) """
  return torch.cos(10*(1/T)*x), True, f"cos(10*{name})"

def sin11x(x, T, name="_"):
  """ f(x) = sin(11x) """
  return torch.sin(11*(1/T)*x), True, f"sin(11*{name})"


def cos11x(x, T, name="_"):
  """ f(x) = cos(11x) """
  return torch.cos(11*(1/T)*x), True, f"cos(11*{name})"

def sin12x(x, T, name="_"):
  """ f(x) = sin(12x) """
  return torch.sin(12*(1/T)*x), True, f"sin(12*{name})"


def cos12x(x, T, name="_"):
  """ f(x) = cos(12x) """
  return torch.cos(12*(1/T)*x), True, f"cos(12*{name})"

def sin13x(x, T, name="_"):
  """ f(x) = sin(13x) """
  return torch.sin(13*(1/T)*x), True, f"sin(13*{name})"


def cos13x(x, T, name="_"):
  """ f(x) = cos(13x) """
  return torch.cos(13*(1/T)*x), True, f"cos(13*{name})"

def sin14x(x, T, name="_"):
  """ f(x) = sin(14x) """
  return torch.sin(14*(1/T)*x), True, f"sin(14*{name})"


def cos14x(x, T, name="_"):
  """ f(x) = cos(14x) """
  return torch.cos(14*(1/T)*x), True, f"cos(14*{name})"

def sin15x(x, T, name="_"):
  """ f(x) = sin(15x) """
  return torch.sin(15*(1/T)*x), True, f"sin(15*{name})"


def cos15x(x, T, name="_"):
  """ f(x) = cos(15x) """
  return torch.cos(15*(1/T)*x), True, f"cos(15*{name})"


def sin16x(x, T, name="_"):
  """ f(x) = sin(16x) """
  return torch.sin(16*(1/T)*x), True, f"sin(16*{name})"


def cos16x(x, T, name="_"):
  """ f(x) = cos(16x) """
  return torch.cos(16*(1/T)*x), True, f"cos(16*{name})"


def sin17x(x, T, name="_"):
  """ f(x) = sin(17x) """
  return torch.sin(17*(1/T)*x), True, f"sin(17*{name})"


def cos17x(x, T, name="_"):
  """ f(x) = cos(17x) """
  return torch.cos(17*(1/T)*x), True, f"cos(17*{name})"


def sin18x(x, T, name="_"):
  """ f(x) = sin(18x) """
  return torch.sin(18*(1/T)*x), True, f"sin(18*{name})"


def cos18x(x, T, name="_"):
  """ f(x) = cos(18x) """
  return torch.cos(18*(1/T)*x), True, f"cos(18*{name})"


def sin19x(x, T, name="_"):
  """ f(x) = sin(19x) """
  return torch.sin(19*(1/T)*x), True, f"sin(19*{name})"


def cos19x(x, T, name="_"):
  """ f(x) = cos(19x) """
  return torch.cos(19*(1/T)*x), True, f"cos(19*{name})"


def sin20x(x, T, name="_"):
  """ f(x) = sin(20x) """
  return torch.sin(20*(1/T)*x), True, f"sin(20*{name})"


def cos20x(x, T, name="_"):
  """ f(x) = cos(20x) """
  return torch.cos(20*(1/T)*x), True, f"cos(20*{name})"
